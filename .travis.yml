language: python
python:
  - "2.6"
  - "2.7"
# command to install dependencies
install:
  - pip install -r test_requirements.txt . --use-mirrors
# command to run tests
script: nosetests 
before_script:
  - psql -c 'create database coaster;' -U postgres
notifications:
  email: false
  irc: "irc.freenode.net#hasgeek-dev"
